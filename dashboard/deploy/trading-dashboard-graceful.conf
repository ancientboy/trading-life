# systemd drop-in: graceful uvicorn shutdown (merged with override.conf on server)
[Service]
TimeoutStopSec=20
KillSignal=SIGTERM
ExecStart=
ExecStart=/usr/bin/python3 -m uvicorn api:app --host 0.0.0.0 --port 9095 --timeout-graceful-shutdown 10
